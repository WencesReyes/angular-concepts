import { FisrtLastWordPipe } from './first-last-word.pipe';

describe('FisrtLastWordPipe', () => {
  it('create an instance', () => {
    const pipe = new FisrtLastWordPipe();
    expect(pipe).toBeTruthy();
  });
});
